
<div class="user-form-container">
  <% if @user.errors.any? && @user.new_record?%>
    <h1>Crear usuario</h1>
  <%else%>
    <h1>Editar usuario</h1>
  <%end%>
    <%= form_for(@user, :html => {class: 'form-user', role: 'form'}) do |f| %>
      <div class="form-group">
        <div class="control-label">
          <%= f.label :nombre %>
        </div>
        <div>
          <%= f.text_field :name, class: 'form-control', placeholder: 'User name', autofocus: true%>
        </div>
      </div>
      <div class="form-group">
        <div class="control-label">
          <%= f.label :email %><br/>
        </div>
        <div>
          <%= f.email_field :email, class: 'form-control', placeholder: 'User email'%>
        </div>
      </div>
      <div class="form-group">
        <div class="control-label">
          <%= f.label :contraseña%><br/>
        </div>
        <div>
          <%= f.password_field :password, class: 'form-control', placeholder: 'Enter password'%>
        </div>
      </div>
      <div class="form-group">
        <%= f.submit(@user.new_record? ? "Registrarse" : "Actualizar cuenta", class: 'btn btn-primary btn-lg submitbtn')%>
      </div>
    <%end%>

  <%= link_to 'Volver', users_path, class:'btn-danger' %>
</div>
