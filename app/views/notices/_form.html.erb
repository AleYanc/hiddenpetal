<div class="notice-form-container">
  <h1>Nueva noticia</h1>
  <%= form_for(@notice, :html => {class: 'notice-form', role: 'form'}) do |form| %>
    <% if notice.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

        <ul>
          <% notice.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label 'Título' %>
      <%= form.text_field :title %>
    </div>

    <div class="field">
      <%= form.label 'Contenido' %>
      <%= form.text_area :desc, class:'notice-textarea' %>
    </div>

    <div class="actions">
      <%= form.submit 'Crear noticia'%>
    </div>
  <% end %>
</div>
